<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Employees</title>
</head>
<body>
  <h1>Employees</h1>

  <ul>
    {{#each employees}}
      <li>
        <a href="/employee/{{this._id}}">
          {{this.name}} ({{this._id}})
        </a>
      </li>
    {{/each}}
  </ul>
</body>
</html>